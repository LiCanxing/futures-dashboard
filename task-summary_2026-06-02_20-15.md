# 修复加权指数查询 BUG + 数据纠正

**时间**：2026-06-02 20:15
**触发**：老板发现红枣数据不对，怀疑拉的是非加权指数数据

## 根因

`daily_update.py` 使用 `hithink_query(f'{name} 历史每日收盘价 持仓量')` 查询品种历史数据。对于郑商所和大商所部分品种，此查询返回多条结果（不同合约），脚本 `hdata[0]` 取了第一条（往往是近月合约），而非加权指数。

例如：查询"红枣"返回 6 条结果（CJ607/CJ609/CJ611/CJ601/CJ603/CJ8888），第一条 CJ607 是近月合约。

## 修复

1. **脚本修复**：`daily_update.py` 第 75 行，查询改为 `hithink_query(f'{name}加权 历史每日收盘价 持仓量')`
2. **数据纠正**：重新拉取 10 个受影响品种（红枣、白糖、橡胶、聚丙烯、PVC、粳米、纤维板、玻璃、菜粕、短纤），全部从 ~233 错误数据点升级为正确的全量历史数据（1000-7000 点）
3. **看板重建**：6369KB，45 品种全量干净数据
4. **Git 推送**：commit `982e3f7`

## 影响

修复前有 12 个品种（今天拉取的）使用了错误数据，部分错误很离谱（如纤维板 OI=4 手、橡胶 OI=213 手）。
修复后全部 45 品种数据干净，无脏数据。
